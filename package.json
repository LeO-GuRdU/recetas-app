{
  "name": "express-app-master",
  "version": "0.0.1",
  "description": "A test in express app",
  "author": "leogurdulich@gmail.com",
  "private": true,
  "scripts": {
    "clean": "find . -name 'dist' -type d -prune -exec rm -rf '{}' + && find . -name 'node_modules' -type d -prune -exec rm -rf '{}' + && find . -name '.angular' -type d -prune -exec rm -rf '{}' +",
    "start": "pm2 start ecosystem.config.js"
  },
  "dependencies": {
    "glob": "^11.0.0",
    "pm2": "^5.4.2"
  }
}
